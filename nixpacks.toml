[variables]
NPM_CONFIG_LEGACY_PEER_DEPS = "true"
                        CACHE_BUST = "nixpacks-fresh-v5"

[phases.install]
cmds = ['npm install']

[phases.build]
        cmds = ['echo CACHE_BUST_V6 && rm -rf dist node_modules/.vite && npm run build']
