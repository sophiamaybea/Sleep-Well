[variables]
NPM_CONFIG_LEGACY_PEER_DEPS = "true"
CACHE_BUST = "nixpacks-fresh-v7"

[phases.install]
cmds = ['npm install --include=dev']

[phases.build]
cmds = ['echo CACHE_BUST_V7 && rm -rf dist node_modules/.vite && npm run build']
