[variables]
NPM_CONFIG_LEGACY_PEER_DEPS = "true"
CACHE_BUST = "v2-deploy-fix"

[phases.install]
cmds = ['npm install --include=dev']

[phases.build]
cmds = ['rm -rf dist node_modules/.vite && npm run build']

[start]
cmd = 'npm start'
